query feed($id: String!) {
  feed(id: $id) {
    id
    name
    address
    lastResult
    network
    label
    requests {
      feedId,
      result,
      drTxHash,
      requestId,
      timestamp,
    }
  }
}