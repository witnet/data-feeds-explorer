query feed($id: String!) {
  feed(id: $id) {
    id
    name
    address
    lastResult
    requests {
      feedId,
      result,
      drTxHash,
      label,
      address,
      requestId,
      timestamp,
    }
  }
}