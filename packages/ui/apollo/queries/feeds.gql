query feeds ($page: Int!, $pageSize: Int!, $network: String!) {
  feeds (page: $page, pageSize: $pageSize, network: $network) {
    feeds {
      feedFullName
      name
      address
      lastResult
      lastResultTimestamp
      network
      label
      blockExplorer
      color
    }
    total
  }
}