query feeds ($page: Int!, $pageSize: Int!) {
  feeds (page: $page, pageSize: $pageSize) {
    feeds {
      feedFullName
      name
      address
      lastResult
      network
      label
      blockExplorer
      color
    }
    total
  }
}