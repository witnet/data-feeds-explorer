query feedsPage ($page: Int!, $pageSize: Int!) {
  feedsPage (page: $page, pageSize: $pageSize) {
    feeds {
      id
      name
      address
      lastResult
      network
      label
    }
    total
  }
}